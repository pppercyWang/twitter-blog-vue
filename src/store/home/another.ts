import { Commit } from 'vuex';

const state: any = {
    // state...
};
const getters = {
    // getters...
};
const mutations = {
};
const actions = {
    // actions...
};
export default {
    state,
    getters,
    mutations,
    actions,
};
